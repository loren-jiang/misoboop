document.addEventListener('DOMContentLoaded', function () {
    const instances = M.Modal.init($("#subscribe_modal"), {
        opacity: .5,
    });

    const instance = M.Modal.getInstance($("#subscribe_modal"));


});