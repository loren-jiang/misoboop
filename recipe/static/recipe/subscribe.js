document.addEventListener('DOMContentLoaded', function () {
    // const elems = document.querySelectorAll('.modal');
    // const instances = M.Modal.init(elems, {});

      const instance = M.Modal.getInstance($("#subscribe_modal"));



});